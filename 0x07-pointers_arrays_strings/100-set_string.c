#include "main.h"
/**
 * set_string- sets value of a pointer to a char
 * @s: pointer to  a pointer of string
 * @to: pointer to the string set s
 * Return: description of returned value
 */
void set_string(char **s, char *to)
{
	*s = to;
}
